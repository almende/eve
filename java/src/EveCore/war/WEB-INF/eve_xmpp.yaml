# Eve XMPP settings

services:
- class: XmppService
  host: openid.almende.org
  port: 5222
  serviceName: openid.almende.org

# context settings
# the context is used by agents for storing their state.
context:
  class: FileContextFactory
  path: .eveagents
