# Eve XMPP settings

# environment settings
environment:
  Development:
    servlet_url: http://localhost:8080/EveCore/agents  # TODO: be able to leave servlet_url undefined
    messenger:
      class: com.almende.eve.messenger.XMPPMessenger
      host: 10.10.1.148
      port: 5222
      service: jos-virtualbox

  Production:
    servlet_url: http://localhost:8080/EveCore/agents  # TODO: be able to leave servlet_url undefined
    messenger:
      class: com.almende.eve.messenger.XMPPMessenger
      host: 10.10.1.148
      port: 5222
      service: jos-virtualbox

agents:
  - class: com.almende.eve.agent.example.TestAgent
    stateful: true
    thread_safe: true

# context settings
# the context is used by agents for storing their state.
context:
  # class: com.almende.eve.context.MemoryContextFactory
  class: com.almende.eve.context.FileContextFactory
  path: .eveagents
