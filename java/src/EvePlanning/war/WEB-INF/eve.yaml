# Eve settings

# environment settings
environment:
  Development:
    servlet_url: http://localhost:8888/EvePlanning/agents
  Production:
    servlet_url: http://localhost:8888/EvePlanning/agents

# agent settings
agent:
  classes:
  - com.almende.eve.agent.google.GoogleCalendarAgent

# context settings
# the context is used by agents for storing their state.
context:
  class: com.almende.eve.context.MemoryContextFactory
